config {
    type: "view",
    dataset: "sources",
    name: "inline_js",
    description: "This is a sample view with inline JS"
}

js {
    const foo = 1;
}

js { 
    const bar = 2; 
}

SELECT ${foo} as one,
       ${bar} as two,
       'three' as three